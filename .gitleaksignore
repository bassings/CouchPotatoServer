# Gitleaks fingerprint baseline — known, assessed, pre-existing findings.
#
# Rules for this file: one entry per finding, each with a comment saying what it
# is and why it is accepted. Never add an entry to make CI green without
# assessing it — an unexplained fingerprint here is indistinguishable from a
# suppressed real leak. Prefer fixing or rotating the credential over adding a
# line. Regenerate/refresh fingerprints with:
#
#   make check-secrets
#
# No findings are currently baselined here. Two used to be:
#
# - The fanart.tv v3 API key baked into
#   couchpotato/core/media/movie/providers/info/fanarttv.py:18 was removed and
#   replaced with the existing per-install `fanart.tv` `api_key` setting (see
#   docs/technical-debt.md) — fixed rather than suppressed, so its baseline
#   entry is gone too.
# - The YouTube key that used to be listed here is gone: its file,
#   movie.actions.js, was an orphan left behind by UI-CLEANUP-02 and has now been
#   deleted outright, which is the better fix than suppressing it.
