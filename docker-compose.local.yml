services:
  couchpotato:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: couchpotato-dev
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Australia/Brisbane
    volumes:
      - /tmp/config:/config
    ports:
      - "5050:5050"
    restart: "no"
